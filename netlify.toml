[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

# Force a specific path to the Netlify CLI's Node binary
[functions]
  node_bundler = "esbuild" 